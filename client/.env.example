# Next.js Client API Endpoint
# Default connects to the local Express backend on Port 5000
NEXT_PUBLIC_API_URL=http://localhost:5000/api/v1

# Disable Turbopack compiler to prevent EPERM file locking errors on Windows
NEXT_DISABLE_TURBOPACK=1
