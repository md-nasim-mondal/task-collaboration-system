/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  /* We can specify rewrites or headers if needed, but standard config is great */
};

export default nextConfig;
