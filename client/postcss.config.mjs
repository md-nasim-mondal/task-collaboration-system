// Tailwind CSS v4 — PostCSS plugin has moved to @tailwindcss/postcss
// autoprefixer is no longer needed; it's handled internally by Tailwind v4
export default {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};
